Create a virtural enviroment: python3 -m venv venv

activate the venv(Linux): source venv/bin/activate 

Need Install: python3 -m pip install django djangorestframework 

To run server: python3 manage.py runserver
